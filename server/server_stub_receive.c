#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <netinet/in.h>
#include "../packet/checksum.h"
#include "../lib/liblog.h"
#include "../packet/execute.h"

Packet_Seq* Unpack_Execute(Packet_Seq *Head)
{
    //char logmsg[128]; snprintf(logmsg, sizeof(logmsg), "Enter Server Stub, unpacking parameters\n",sockfd);
    //logging(LOGFILE, logmsg);
    //TODO: implemenation of unpack. unpack stuff into input file 
    
    return Head;
}
